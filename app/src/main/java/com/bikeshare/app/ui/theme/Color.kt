package com.bikeshare.app.ui.theme

import androidx.compose.ui.graphics.Color

// Amora brand colors
val Blue100  = Color(0xFF0057FF)  // primary — electric blue
val Blue60   = Color(0xFF00C2FF)  // secondary — sky blue accent
val Blue10   = Color(0xFFE8F0FF)  // surface tint / light backgrounds

val Charcoal = Color(0xFF1A1A2E)  // dark text
val SlateGrey = Color(0xFF6B7280) // secondary text / subtitles

val White    = Color(0xFFFFFFFF)
val Surface  = Color(0xFFF4F6FA)  // card / screen backgrounds
val Error    = Color(0xFFE53935)
